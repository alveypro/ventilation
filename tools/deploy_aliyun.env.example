ALIYUN_ACCESS_KEY_ID=
ALIYUN_ACCESS_KEY_SECRET=
ALIYUN_REGION=cn-hangzhou

ROOT_DOMAIN=airivo.cn
ENABLE_WWW=1
ENABLE_API_DOMAIN=1

ECS_INSTANCE_TYPE=ecs.c6.large
ECS_DISK_SIZE=200
ECS_LOGIN_USER=ubuntu

# Optional: set to reuse existing network resources
# VPC_ID=
# VSWITCH_ID=
# SECURITY_GROUP_ID=

# Optional: if not set, script will create/import a key pair
SSH_KEY_NAME=airivo-deploy-key
SSH_KEY_PATH=/Users/mac/ventilation/tools/keys/airivo-deploy-key

# Backend secrets (set strong values)
JWT_SECRET=
MONGO_ROOT_USER=airivo_admin
MONGO_ROOT_PASSWORD=
MONGO_DB=ventilation

# Optional: enable HTTPS via certbot (requires DNS records resolved)
ENABLE_HTTPS=0
